import express from 'express';
import cors from 'cors';
import crypto from 'crypto';
import { supabase } from './supabase.js';
import { ALL_SYSTEM_PERMISSIONS, getUserById, getUserByEmail, listUsers, listDepartments, getOrCreateDepartment, createDepartment, updateDepartment, listBosses, getBossById, createBoss, updateBoss, deleteBoss, listRoles, createRole, updateRole, getPopulatedRequests, getRequest, insertRequest, updateRequest, deleteRequest, generateNextFolio, createApprovalToken, validateApprovalToken, processApprovalTokenAction, recordAuditLog, listAuditLogs, createVerificationCode, verifyCodeAndActivateUser, hashPassword, verifyPassword, sanitizeUser, hasPermission } from './db';
import { buildBossApprovalEmailHtml, buildSystemsApprovedEmailHtml, buildVerificationEmailHtml, buildNewAccountAdminEmailHtml, buildTokenApprovalResultPageHtml, sendEmail, getFromAddress, outboxLogs } from './mailService';
import type { User, Role, TravelRequest, StoredUserRecord } from '../src/types';

function baseUrl(req:express.Request){const configured=process.env.APP_URL?.trim().replace(/\/+$/,'');if(configured&&!(process.env.VERCEL&&configured.includes('localhost')))return configured;if(process.env.VERCEL_PROJECT_PRODUCTION_URL)return `https://${process.env.VERCEL_PROJECT_PRODUCTION_URL}`;if(process.env.VERCEL_URL)return `https://${process.env.VERCEL_URL}`;const host=req.headers['x-forwarded-host']||req.headers.host;const proto=String(req.headers['x-forwarded-proto']||'http').split(',')[0];return host?`${proto}://${host}`:'http://localhost:3000';}
function parseCookies(req:express.Request){const raw=req.headers.cookie||'';return Object.fromEntries(raw.split(';').map(x=>x.trim()).filter(Boolean).map(x=>{const i=x.indexOf('=');return i<0?[x,'']:[x.slice(0,i),decodeURIComponent(x.slice(i+1))];}));}
function jwtSign(payload:Record<string,unknown>){const secret=process.env.JWT_SECRET; if(!secret)throw new Error('Falta JWT_SECRET');const enc=(v:string)=>Buffer.from(v).toString('base64url');const head=enc(JSON.stringify({alg:'HS256',typ:'JWT'}));const body=enc(JSON.stringify({...payload,iat:Math.floor(Date.now()/1000),exp:Math.floor(Date.now()/1000)+8*60*60}));const sig=crypto.createHmac('sha256',secret).update(`${head}.${body}`).digest('base64url');return `${head}.${body}.${sig}`;}
function jwtVerify(token:string){const secret=process.env.JWT_SECRET;if(!secret)throw new Error('Falta JWT_SECRET');const p=token.split('.');if(p.length!==3)throw new Error('JWT inválido');const expected=crypto.createHmac('sha256',secret).update(`${p[0]}.${p[1]}`).digest('base64url');if(!crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(p[2])))throw new Error('JWT inválido');const payload=JSON.parse(Buffer.from(p[1],'base64url').toString('utf8'));if(!payload.exp||payload.exp<Math.floor(Date.now()/1000))throw new Error('Sesión expirada');return payload as {sub:string};}
async function auth(req:express.Request){const cookies=parseCookies(req);const bearer=String(req.headers.authorization||'').replace(/^Bearer\s+/i,'').trim();const token=cookies.dimer_session||bearer;if(!token)return null;try{const p=jwtVerify(token);const u=await getUserById(p.sub);if(!u||u.status!=='ACTIVO')return null;const roles=await listRoles();return sanitizeUser(u,roles.find(r=>r.id===u.roleId));}catch{return null;}}
function requireAuth(req:express.Request,res:express.Response,next:express.NextFunction){void auth(req).then(u=>{if(!u)return res.status(401).json({error:'Autenticación requerida'});(req as any).dimerUser=u;next();}).catch(()=>res.status(503).json({error:'Base de datos no disponible'}));}
function requirePermission(permission:any){return (req:express.Request,res:express.Response,next:express.NextFunction)=>{const u=(req as any).dimerUser as User|undefined;if(!hasPermission(u,permission))return res.status(403).json({error:'No tienes permiso para esta operación'});next();};}
function err(res:express.Response,e:unknown){const msg=e instanceof Error?e.message:'Error interno';const status=/JWT|autentic|token inválido|no se encontró/i.test(msg)?400:503;return res.status(status).json({success:false,error:msg});}

export function createApp(){const app=express();app.set('trust proxy',1);app.use(cors({origin:true,credentials:true}));app.use(express.json({limit:'10mb'}));
app.get(['/api/health','/health'],async(_req,res)=>{try{const {error}=await supabase.from('users').select('id',{head:true,count:'exact'});if(error)throw error;res.json({ok:true,service:'viaticos-dimer-api',database:'supabase',runtime:process.env.VERCEL?'vercel-serverless':'node',timestamp:new Date().toISOString()});}catch(e){res.status(503).json({ok:false,database:'supabase',error:'Database unavailable'});}});
app.get(['/api/diagnostic','/diagnostic'],async(_req,res)=>{try{const [u,r]=await Promise.all([supabase.from('users').select('id',{head:true,count:'exact'}),supabase.from('travel_requests').select('id',{head:true,count:'exact'})]);if(u.error)throw u.error;if(r.error)throw r.error;res.json({status:'operational',runtime:process.env.VERCEL?'vercel-serverless':'node',database:{type:'supabase',usersCount:u.count||0,requestsCount:r.count||0,persistenceType:'supabase-postgresql'},environmentChecks:{isVercel:Boolean(process.env.VERCEL),hasAppUrl:Boolean(process.env.APP_URL),hasJwtSecret:Boolean(process.env.JWT_SECRET),hasSmtpHost:Boolean(process.env.SMTP_HOST||process.env.EMAIL_HOST),hasSmtpUser:Boolean(process.env.SMTP_USER||process.env.GMAIL_USER),hasSmtpPass:Boolean(process.env.SMTP_PASS||process.env.SMTP_PASSWORD),finanzasEmailConfigured:Boolean(process.env.FINANZAS_EMAIL)}});}catch(e){res.status(503).json({status:'unavailable',database:{type:'supabase'},error:e instanceof Error?e.message:'Database unavailable'});}});
app.get(['/api/me','/me'],async(req,res)=>{try{const u=await auth(req);const allUsers=await listUsers();res.json({user:u,allUsers,appUrl:baseUrl(req),finanzasEmail:process.env.FINANZAS_EMAIL||'finanzas@dimer.com.mx',systemsEmail:'sistemas@dimer.com.mx'});}catch(e){err(res,e);}});
app.post(['/api/auth/login','/auth/login','/api/login','/login'],async(req,res)=>{try{const email=String(req.body?.email||'').trim().toLowerCase(),password=String(req.body?.password||'');if(!email||!password)return res.status(400).json({success:false,error:'Correo electrónico y contraseña requeridos'});const u=await getUserByEmail(email);if(!u)return res.status(401).json({success:false,error:'No existe una cuenta registrada con este correo.'});if(u.status!=='ACTIVO')return res.status(403).json({success:false,error:'Esta cuenta está inactiva.'});if(!verifyPassword(password,u.passwordHash,u.salt))return res.status(401).json({success:false,error:'Contraseña incorrecta.'});const roles=await listRoles();const user=sanitizeUser(u,roles.find(r=>r.id===u.roleId));const token=jwtSign({sub:u.id,email:u.email});res.cookie('dimer_session',token,{httpOnly:true,secure:process.env.NODE_ENV==='production'||Boolean(process.env.VERCEL),sameSite:'lax',path:'/',maxAge:8*60*60*1000});await recordAuditLog({userId:u.id,action:'INICIO_SESION',details:{email:u.email,role:u.role}});res.json({success:true,user});}catch(e){err(res,e);}});
app.post('/api/auth/logout',async(req,res)=>{try{const u=await auth(req);if(u)await recordAuditLog({userId:u.id,action:'CIERRE_SESION',details:{email:u.email}});res.setHeader('Set-Cookie','dimer_session=; Max-Age=0; Path=/; HttpOnly; SameSite=Lax');res.json({success:true});}catch(e){err(res,e);}});
app.post('/api/switch-user',(_req,res)=>res.status(403).json({error:'Cambio de usuario deshabilitado en producción. Inicia sesión con credenciales reales.'}));
app.post('/api/auth/register-init',async(req,res)=>{try{const {name,email,password,department}=req.body||{};const clean=String(email||'').trim().toLowerCase();if(!name||!clean||!password)return res.status(400).json({error:'Nombre, correo y contraseña son obligatorios'});if(await getUserByEmail(clean))return res.status(400).json({error:'Ya existe una cuenta registrada con este correo.'});const dept=await getOrCreateDepartment(String(department||'General'));const {hash,salt}=hashPassword(String(password));const {code,expiresAt}=await createVerificationCode({email:clean,name:String(name),department:dept.name,roleId:'role_solicitante',passwordHash:hash,salt});const mail=await sendEmail({to:clean,subject:`Código de Verificación Dimer: ${code}`,html:buildVerificationEmailHtml({name:String(name),email:clean,code,expiresMinutes:15})});if(mail.status==='FALLIDO')return res.status(500).json({error:`Error SMTP: ${mail.error||'fallo de envío'}`});res.json({success:true,email:clean,expiresAt,simulatedCode:mail.status==='SIMULADO'?code:undefined});}catch(e){err(res,e);}});
app.post('/api/auth/verify-code',async(req,res)=>{try{const result=await verifyCodeAndActivateUser(String(req.body?.email||''),String(req.body?.code||''));if(!result.success||!result.user)return res.status(400).json({error:result.error});const roles=await listRoles();const user=sanitizeUser(result.user,roles.find(r=>r.id===result.user?.roleId));const token=jwtSign({sub:result.user.id,email:result.user.email});res.cookie('dimer_session',token,{httpOnly:true,secure:process.env.NODE_ENV==='production'||Boolean(process.env.VERCEL),sameSite:'lax',path:'/',maxAge:8*60*60*1000});await recordAuditLog({userId:result.user.id,action:'VERIFICACION_Y_ACTIVACION_CUENTA',details:{email:user.email}});try{await sendEmail({to:'sistemas@dimer.com.mx',subject:`NUEVA CUENTA REGISTRADA - ${user.name}`,html:buildNewAccountAdminEmailHtml({user,registeredAt:new Date().toISOString()})});}catch{}res.json({success:true,user,message:'Cuenta verificada y activada con éxito.'});}catch(e){err(res,e);}});
app.post('/api/auth/resend-code',async(req,res)=>{try{const clean=String(req.body?.email||'').trim().toLowerCase();const {data,error}=await supabase.from('verification_codes').select('*').eq('email',clean).maybeSingle();if(error)throw error;if(!data)return res.status(404).json({error:'No hay un registro pendiente para este correo.'});const code=crypto.randomInt(100000,1000000).toString();const expiresAt=new Date(Date.now()+15*60*1000).toISOString();await supabase.from('verification_codes').update({code,expires_at:expiresAt,attempts:0}).eq('email',clean);const mail=await sendEmail({to:clean,subject:`Nuevo Código de Verificación Dimer: ${code}`,html:buildVerificationEmailHtml({name:data.name,email:clean,code,expiresMinutes:15})});if(mail.status==='FALLIDO')return res.status(500).json({error:mail.error||'Fallo SMTP'});res.json({success:true,expiresAt,simulatedCode:mail.status==='SIMULADO'?code:undefined});}catch(e){err(res,e);}});
app.post('/api/auth/register',requireAuth,requirePermission('administrar_usuarios'),async(req,res)=>{try{const {name,email,password,department,roleId,status}=req.body||{};const clean=String(email||'').trim().toLowerCase();if(!name||!clean||!password)return res.status(400).json({error:'Nombre, correo y contraseña son obligatorios'});if(await getUserByEmail(clean))return res.status(400).json({error:'Ya existe un usuario registrado con este correo.'});const dept=await getOrCreateDepartment(String(department||'General'));const {hash,salt}=hashPassword(String(password));const roleDef=(await listRoles()).find(r=>r.id===roleId)||{id:'role_solicitante',name:'Solicitante',description:'',active:true,isSystem:true,permissions:['ver_solicitudes']};const role:Role=roleDef.id==='role_admin'?'ADMIN':roleDef.id==='role_jefe'?'JEFE':roleDef.id==='role_finanzas'?'FINANZAS':roleDef.id==='role_solo_lectura'?'SOLO_LECTURA_APROBADAS':'SOLICITANTE';const u=await (await import('./db')).insertUser({id:`usr_${Date.now()}_${crypto.randomBytes(5).toString('hex')}`,name:String(name).trim(),email:clean,role,roleId:roleDef.id,department:dept.name,status:status==='INACTIVO'?'INACTIVO':'ACTIVO',isVerified:true,passwordHash:hash,salt,createdAt:new Date().toISOString()});await recordAuditLog({userId:(req as any).dimerUser.id,action:'REGISTRO_USUARIO_ADMIN',details:{newUserId:u.id,newUserEmail:u.email}});res.status(201).json({success:true,user:sanitizeUser(u,roleDef)});}catch(e){err(res,e);}});

app.get('/api/users',requireAuth,requirePermission('administrar_usuarios'),async(_req,res)=>{try{res.json(await listUsers());}catch(e){err(res,e);}});
app.post('/api/users',requireAuth,requirePermission('administrar_usuarios'),async(req,res)=>{try{const {name,email,password,department,roleId,status}=req.body||{};const clean=String(email||'').trim().toLowerCase();if(await getUserByEmail(clean))return res.status(400).json({error:'El correo electrónico ya se encuentra en uso.'});const dept=await getOrCreateDepartment(String(department||'General'));const roleDef=(await listRoles()).find(r=>r.id===roleId)||{id:'role_solicitante',name:'Solicitante',description:'',active:true,isSystem:true,permissions:[]};const role:Role=roleDef.id==='role_admin'?'ADMIN':roleDef.id==='role_jefe'?'JEFE':roleDef.id==='role_finanzas'?'FINANZAS':roleDef.id==='role_solo_lectura'?'SOLO_LECTURA_APROBADAS':'SOLICITANTE';const {hash,salt}=hashPassword(String(password||'password123'));const u=await (await import('./db')).insertUser({id:`usr_${Date.now()}_${crypto.randomBytes(5).toString('hex')}`,name:String(name).trim(),email:clean,role,roleId:roleDef.id,department:dept.name,status:status==='INACTIVO'?'INACTIVO':'ACTIVO',isVerified:true,passwordHash:hash,salt,createdAt:new Date().toISOString()});await recordAuditLog({userId:(req as any).dimerUser.id,action:'CREACION_USUARIO_ADMIN',details:{newUserId:u.id,newUserEmail:u.email}});res.status(201).json({success:true,user:sanitizeUser(u,roleDef)});}catch(e){err(res,e);}});
app.put('/api/users/:id',requireAuth,requirePermission('administrar_usuarios'),async(req,res)=>{try{const id=String(req.params.id);const existing=await getUserById(id);if(!existing)return res.status(404).json({error:'Usuario no encontrado'});const p:any={};for(const k of ['name','email','department','status'])if(req.body[k]!==undefined)p[k]=req.body[k];if(req.body.roleId)p.roleId=req.body.roleId;if(req.body.password){const h=hashPassword(String(req.body.password));p.passwordHash=h.hash;p.salt=h.salt;}if(req.body.roleId){const rd=(await listRoles()).find(r=>r.id===req.body.roleId);p.role=rd?.id==='role_admin'?'ADMIN':rd?.id==='role_jefe'?'JEFE':rd?.id==='role_finanzas'?'FINANZAS':rd?.id==='role_solo_lectura'?'SOLO_LECTURA_APROBADAS':'SOLICITANTE';}const u=await (await import('./db')).updateUser(id,p);await recordAuditLog({userId:(req as any).dimerUser.id,action:'ACTUALIZACION_USUARIO',details:{targetUserId:id}});res.json({success:true,user:sanitizeUser(u)});}catch(e){err(res,e);}});
app.delete('/api/users/:id',requireAuth,requirePermission('administrar_usuarios'),async(req,res)=>{try{const u=await getUserById(String(req.params.id));if(!u)return res.status(404).json({error:'Usuario no encontrado'});if(u.email.toLowerCase()==='sistemas@dimer.com.mx')return res.status(400).json({error:'No se puede eliminar la cuenta principal de administración'});await (await import('./db')).deleteUser(u.id);await recordAuditLog({userId:(req as any).dimerUser.id,action:'ELIMINACION_USUARIO',details:{deletedUserId:u.id,email:u.email}});res.json({success:true});}catch(e){err(res,e);}});

app.get('/api/departments',async(_req,res)=>{try{res.json(await listDepartments());}catch(e){err(res,e);}});
app.post('/api/departments',requireAuth,requirePermission('administrar_departamentos'),async(req,res)=>{try{const d=await createDepartment(String(req.body?.name||''),String(req.body?.description||''));await recordAuditLog({userId:(req as any).dimerUser.id,action:'CREACION_DEPARTAMENTO',details:{departmentName:d.name}});res.status(201).json({success:true,department:d});}catch(e){err(res,e);}});
app.put('/api/departments/:id',requireAuth,requirePermission('administrar_departamentos'),async(req,res)=>{try{res.json({success:true,department:await updateDepartment(String(req.params.id),req.body)});}catch(e){err(res,e);}});
app.get('/api/bosses',async(_req,res)=>{try{res.json(await listBosses());}catch(e){err(res,e);}});
app.post('/api/bosses',requireAuth,requirePermission('administrar_jefes'),async(req,res)=>{try{const b=await createBoss(String(req.body?.name||''),String(req.body?.email||''),String(req.body?.department||''));res.status(201).json({success:true,boss:b});}catch(e){err(res,e);}});
for(const method of ['put','patch','post'] as const)app[method]('/api/bosses/:id',requireAuth,requirePermission('administrar_jefes'),async(req,res)=>{try{res.json({success:true,boss:await updateBoss(String(req.params.id),req.body)});}catch(e){err(res,e);}});
app.delete('/api/bosses/:id',requireAuth,requirePermission('administrar_jefes'),async(req,res)=>{try{await deleteBoss(String(req.params.id));res.json({success:true});}catch(e){err(res,e);}});
app.get('/api/roles',async(_req,res)=>{try{res.json(await listRoles());}catch(e){err(res,e);}});app.get('/api/permissions',(_req,res)=>res.json(ALL_SYSTEM_PERMISSIONS));
app.post('/api/roles',requireAuth,requirePermission('administrar_roles'),async(req,res)=>{try{res.status(201).json({success:true,role:await createRole(req.body)});}catch(e){err(res,e);}});app.put('/api/roles/:id',requireAuth,requirePermission('administrar_roles'),async(req,res)=>{try{res.json({success:true,role:await updateRole(String(req.params.id),req.body)});}catch(e){err(res,e);}});

app.get('/api/requests',async(req,res)=>{try{const u=await auth(req);if(!u)return res.status(401).json({error:'Autenticación requerida'});let list=await getPopulatedRequests();const roleFilter=String(req.query.roleFilter||'');const status=String(req.query.status||'');if(u.role==='SOLO_LECTURA_APROBADAS'||u.roleId==='role_solo_lectura')list=list.filter(r=>['APROBADA','PAGADA','FINALIZADA'].includes(r.status));else if(u.role==='SOLICITANTE'&&roleFilter!=='to_approve')list=list.filter(r=>r.userId===u.id);if(roleFilter==='mine')list=list.filter(r=>r.userId===u.id);if(roleFilter==='to_approve')list=list.filter(r=>r.bossEmail?.toLowerCase()===u.email.toLowerCase()||u.role==='ADMIN');if(['finanzas','approved_only'].includes(roleFilter))list=list.filter(r=>['APROBADA','PAGADA','FINALIZADA'].includes(r.status));if(status&&status!=='TODAS')list=list.filter(r=>r.status===status);res.json(list);}catch(e){err(res,e);}});
app.get('/api/requests/:id',requireAuth,async(req,res)=>{try{const r=await getRequest(String(req.params.id));if(!r)return res.status(404).json({error:'Solicitud no encontrada'});const u=(req as any).dimerUser as User;const owner=r.userId===u.id;const canApprove=u.role==='ADMIN'||u.email.toLowerCase()===r.bossEmail.toLowerCase()||hasPermission(u,'aprobar_solicitudes');if(!owner&&!canApprove&&u.role!=='FINANZAS')return res.status(403).json({error:'No autorizado'});const user=await getUserById(r.userId);const logs=await listAuditLogs(r.id);await recordAuditLog({requestId:r.id,userId:u.id,action:'VISUALIZACION_SOLICITUD',details:{folio:r.folio}});res.json({request:{...r,user:user?sanitizeUser(user):undefined},auditLogs:logs,canApprove});}catch(e){err(res,e);}});
app.post('/api/requests',requireAuth,async(req,res)=>{try{const u=(req as any).dimerUser as User;const b=req.body||{};const boss=b.bossId?await getBossById(String(b.bossId)):null;const bossEmail=String(boss?.email||b.bossEmail||'').trim().toLowerCase();if(!bossEmail)return res.status(400).json({error:'Debe seleccionar un jefe que autoriza'});const reason=String(b.detail||b.reason||'').trim();if(!reason)return res.status(400).json({error:'La descripción o detalle es obligatorio'});const amount=Number(b.amountRequested||0);if(!Number.isFinite(amount)||amount<0)return res.status(400).json({error:'Monto solicitado no válido'});const id=`req_${Date.now()}_${crypto.randomBytes(5).toString('hex')}`;const token=await createApprovalToken(id,bossEmail,boss?.id);const folio=await generateNextFolio();const r=await insertRequest({id,folio,status:'PENDIENTE_APROBACION',userId:u.id,requesterName:String(b.requesterName||u.name),department:String(b.department||u.department||'General'),requestType:String(b.requestType||'Viáticos y Gastos de Viaje'),detail:reason,requestDate:String(b.requestDate||new Date().toISOString().slice(0,10)),urgency:String(b.urgency||'media'),bossId:boss?.id,bossEmail,bossName:boss?.name||bossEmail,startDate:String(b.startDate||new Date().toISOString()),endDate:String(b.endDate||b.startDate||new Date().toISOString()),destination:String(b.destination||'Oficina / Centro Corporativo'),reason,amountRequested:amount,amountAuthorized:null,transportCost:Number(b.transportCost||0),hotelCost:Number(b.hotelCost||0),foodCost:Number(b.foodCost||0),miscCost:Number(b.miscCost||0),comments:b.comments?String(b.comments).trim():null,approvalToken:token.token,createdAt:new Date().toISOString()});await recordAuditLog({requestId:r.id,userId:u.id,action:'CREACION_SOLICITUD',details:{folio:r.folio,amountRequested:r.amountRequested,bossEmail}});const approveUrl=`${baseUrl(req)}/api/approval/token-action?token=${encodeURIComponent(token.token)}&action=approve`;const rejectUrl=`${baseUrl(req)}/api/approval/token-action?token=${encodeURIComponent(token.token)}&action=reject`;const html=buildBossApprovalEmailHtml({request:r,user:u,approveUrl,rejectUrl,token:token.token});const mail=await sendEmail({to:bossEmail,subject:`SOLICITUD POR AUTORIZAR - Folio ${folio}`,html,requestId:id,folio});if(bossEmail!=='sistemas@dimer.com.mx')await sendEmail({to:'sistemas@dimer.com.mx',subject:`Nueva solicitud por autorizar - ${folio}`,html,requestId:id,folio});res.status(201).json({success:true,request:r,approvalToken:token.token,mailResult:mail});}catch(e){err(res,e);}});
app.put('/api/requests/:id',requireAuth,async(req,res)=>{try{const u=(req as any).dimerUser as User;const r=await getRequest(String(req.params.id));if(!r)return res.status(404).json({error:'Solicitud no encontrada'});if(u.role!=='ADMIN'&&r.userId!==u.id)return res.status(403).json({error:'No tienes permisos'});if(u.role!=='ADMIN'&&!['PENDIENTE_APROBACION','CORRECCION_SOLICITADA','BORRADOR'].includes(r.status))return res.status(400).json({error:`No se puede modificar en ${r.status}`});const updated=await updateRequest(r.id,{...req.body,updatedAt:new Date().toISOString()});await recordAuditLog({requestId:r.id,userId:u.id,action:'MODIFICACION_SOLICITUD',details:{folio:r.folio}});res.json({success:true,request:updated});}catch(e){err(res,e);}});
app.patch('/api/requests/:id',requireAuth,async(req,res)=>{try{const r=await getRequest(String(req.params.id));if(!r)return res.status(404).json({error:'Solicitud no encontrada'});const u=(req as any).dimerUser as User;if(u.role!=='ADMIN'&&r.userId!==u.id)return res.status(403).json({error:'No autorizado'});res.json({success:true,request:await updateRequest(r.id,{...req.body,updatedAt:new Date().toISOString()})});}catch(e){err(res,e);}});
app.delete('/api/requests/:id',requireAuth,async(req,res)=>{try{const u=(req as any).dimerUser as User;const r=await getRequest(String(req.params.id));if(!r)return res.status(404).json({error:'Solicitud no encontrada'});if(u.role!=='ADMIN'&&r.userId!==u.id)return res.status(403).json({error:'No autorizado'});await deleteRequest(r.id);await recordAuditLog({requestId:r.id,userId:u.id,action:'ELIMINACION_SOLICITUD',details:{folio:r.folio}});res.json({success:true,deletedId:r.id});}catch(e){err(res,e);}});
app.post('/api/requests/:id/cancel',requireAuth,async(req,res)=>{try{const u=(req as any).dimerUser as User;const r=await getRequest(String(req.params.id));if(!r)return res.status(404).json({error:'Solicitud no encontrada'});if(r.userId!==u.id&&u.role!=='ADMIN')return res.status(403).json({error:'No autorizado'});if(['APROBADA','PAGADA','FINALIZADA'].includes(r.status))return res.status(400).json({error:`No se puede cancelar ${r.status}`});const updated=await updateRequest(r.id,{status:'CANCELADA',updatedAt:new Date().toISOString()});await recordAuditLog({requestId:r.id,userId:u.id,action:'CANCELACION_SOLICITUD',details:{folio:r.folio}});res.json({success:true,request:updated});}catch(e){err(res,e);}});

async function tokenAction(req:express.Request,res:express.Response){try{const raw=String(req.query.token||'');const action=String(req.query.action||'').toLowerCase();const decision=action==='reject'||action==='rechazar'?'RECHAZADA':'APROBADA';if(!raw)return res.status(400).send(buildTokenApprovalResultPageHtml({status:'INVALIDA',errorMessage:'Token no proporcionado'}));if(decision==='RECHAZADA'&&!req.query.reason)return res.status(400).send(buildTokenApprovalResultPageHtml({status:'INVALIDA',errorMessage:'Para rechazar, indique el motivo mediante el formulario de aprobación.'}));const comments=typeof req.query.reason==='string'?req.query.reason:'Rechazado directamente desde el correo electrónico';const result=await processApprovalTokenAction(raw,decision,undefined,comments);const r=await getRequest(String(result.requestId));if(!r)return res.status(500).send(buildTokenApprovalResultPageHtml({status:'INVALIDA',errorMessage:'La solicitud procesada no fue encontrada'}));const requester=result.userId?await getUserById(String(result.userId)):null;const user=requester?sanitizeUser(requester):{id:r.userId,name:r.requesterName||'Colaborador',email:'',department:r.department||'General',role:'SOLICITANTE',status:'ACTIVO'} as User;if(decision==='APROBADA'){const html=buildSystemsApprovedEmailHtml({request:r,user,approverName:String(result.bossEmail||'Jefe Aprobador'),approverEmail:String(result.bossEmail||''),approvedAt:r.approvedAt});await sendEmail({to:user.email,subject:`SOLICITUD DE VIÁTICOS APROBADA - ${r.folio}`,html,requestId:r.id,folio:r.folio});await sendEmail({to:'sistemas@dimer.com.mx',subject:`SOLICITUD DE VIÁTICOS APROBADA - ${r.folio}`,html,requestId:r.id,folio:r.folio});const fin=process.env.FINANZAS_EMAIL||'finanzas@dimer.com.mx';if(fin.toLowerCase()!=='sistemas@dimer.com.mx')await sendEmail({to:fin,subject:`SOLICITUD DE VIÁTICOS APROBADA - ${r.folio}`,html,requestId:r.id,folio:r.folio});}else if(user.email){await sendEmail({to:user.email,subject:`SOLICITUD DE VIÁTICOS RECHAZADA - ${r.folio}`,html:`<p>Su solicitud <strong>${r.folio}</strong> fue rechazada.</p><p>${r.comments||comments}</p>`});}return res.send(buildTokenApprovalResultPageHtml({status:decision,request:r,actionTaken:decision,processedBy:String(result.bossEmail||''),processedAt:String(result.processedAt||new Date().toISOString())}));}catch(e){const msg=e instanceof Error?e.message:'Error procesando autorización';return res.status(/ya fue utilizado|expirado|inválido|procesada/i.test(msg)?400:500).send(buildTokenApprovalResultPageHtml({status:'INVALIDA',errorMessage:msg}));}}
app.get('/api/approval/token-action',tokenAction);app.get('/approval-response/:token/:decision',async(req,res)=>{req.query.token=String(req.params.token);req.query.action=String(req.params.decision);return tokenAction(req,res);});app.get('/api/approval-response/:token/:decision',async(req,res)=>{req.query.token=String(req.params.token);req.query.action=String(req.params.decision);return tokenAction(req,res);});
app.get('/api/approval-tokens/:token',async(req,res)=>{try{const v=await validateApprovalToken(String(req.params.token));if(!v.valid)return res.status(400).json({valid:false,error:v.error});const user=await getUserById(v.request.userId);res.json({valid:true,tokenRecord:v.tokenRecord,request:{...v.request,user:user?sanitizeUser(user):undefined}});}catch(e){err(res,e);}});
app.post('/api/requests/:id/approve',requireAuth,async(req,res)=>{try{const u=(req as any).dimerUser as User;const r=await getRequest(String(req.params.id));if(!r)return res.status(404).json({error:'Solicitud no encontrada'});if(u.role!=='ADMIN'&&u.email.toLowerCase()!==r.bossEmail.toLowerCase())return res.status(403).json({error:'No autorizado'});if(!r.approvalToken)return res.status(400).json({error:'La solicitud no tiene token de aprobación'});const result=await processApprovalTokenAction(r.approvalToken,'APROBADA',Number(req.body?.amountAuthorized??r.amountRequested),req.body?.comments||null);const updated=await getRequest(r.id);if(!updated)throw new Error('Solicitud no encontrada después de aprobar');const requester=await getUserById(r.userId);if(requester){const html=buildSystemsApprovedEmailHtml({request:updated,user:sanitizeUser(requester),approverName:u.name,approverEmail:u.email,approvedAt:updated.approvedAt});await sendEmail({to:requester.email,subject:`SOLICITUD DE VIÁTICOS APROBADA - ${updated.folio}`,html,requestId:r.id,folio:r.folio});await sendEmail({to:'sistemas@dimer.com.mx',subject:`SOLICITUD DE VIÁTICOS APROBADA - ${updated.folio}`,html,requestId:r.id,folio:r.folio});}res.json({success:true,request:updated,result});}catch(e){err(res,e);}});
app.post('/api/requests/:id/reject',requireAuth,async(req,res)=>{try{const u=(req as any).dimerUser as User;const r=await getRequest(String(req.params.id));if(!r)return res.status(404).json({error:'Solicitud no encontrada'});if(u.role!=='ADMIN'&&u.email.toLowerCase()!==r.bossEmail.toLowerCase())return res.status(403).json({error:'No autorizado'});if(!String(req.body?.comments||'').trim())return res.status(400).json({error:'El motivo del rechazo es obligatorio'});if(!r.approvalToken)return res.status(400).json({error:'La solicitud no tiene token de aprobación'});const result=await processApprovalTokenAction(r.approvalToken,'RECHAZADA',null,String(req.body.comments).trim());const updated=await getRequest(r.id);if(!updated)throw new Error('Solicitud no encontrada');const requester=await getUserById(r.userId);if(requester)await sendEmail({to:requester.email,subject:`SOLICITUD DE VIÁTICOS RECHAZADA - ${r.folio}`,html:`<p>Su solicitud <strong>${r.folio}</strong> fue rechazada.</p><p>${String(req.body.comments).trim()}</p>`});res.json({success:true,request:updated,result});}catch(e){err(res,e);}});
app.post('/api/requests/:id/request-correction',requireAuth,async(req,res)=>{try{const u=(req as any).dimerUser as User;const r=await getRequest(String(req.params.id));if(!r)return res.status(404).json({error:'Solicitud no encontrada'});if(u.role!=='ADMIN'&&u.email.toLowerCase()!==r.bossEmail.toLowerCase())return res.status(403).json({error:'No autorizado'});const updated=await updateRequest(r.id,{status:'CORRECCION_SOLICITADA',comments:req.body?.comments||r.comments,updatedAt:new Date().toISOString()});await recordAuditLog({requestId:r.id,userId:u.id,action:'SOLICITUD_CORRECCION',details:{notes:req.body?.comments||''}});res.json({success:true,request:updated});}catch(e){err(res,e);}});
app.post('/api/requests/:id/pay',requireAuth,requirePermission('ver_reportes'),async(req,res)=>{try{const r=await getRequest(String(req.params.id));if(!r)return res.status(404).json({error:'Solicitud no encontrada'});const updated=await updateRequest(r.id,{status:'PAGADA',updatedAt:new Date().toISOString()});await recordAuditLog({requestId:r.id,userId:(req as any).dimerUser.id,action:'DISPERSION_PAGO',details:{reference:req.body?.reference||'SPEI-DIRECTO',notes:req.body?.notes||''}});res.json({success:true,request:updated});}catch(e){err(res,e);}});
app.post('/api/requests/:id/finalize',requireAuth,requirePermission('ver_reportes'),async(req,res)=>{try{const r=await getRequest(String(req.params.id));if(!r)return res.status(404).json({error:'Solicitud no encontrada'});const updated=await updateRequest(r.id,{status:'FINALIZADA',updatedAt:new Date().toISOString()});await recordAuditLog({requestId:r.id,userId:(req as any).dimerUser.id,action:'FINALIZACION_COMPROBACION',details:{notes:req.body?.notes||''}});res.json({success:true,request:updated});}catch(e){err(res,e);}});
app.get('/api/audit-logs',requireAuth,requirePermission('administrar_configuracion'),async(req,res)=>{try{res.json(await listAuditLogs(typeof req.query.requestId==='string'?req.query.requestId:undefined));}catch(e){err(res,e);}});
app.get('/api/outbox',requireAuth,async(_req,res)=>res.json(outboxLogs));
app.get('/api/smtp/status',requireAuth,async(_req,res)=>{const host=process.env.SMTP_HOST||process.env.EMAIL_HOST||'smtp.gmail.com';const port=process.env.SMTP_PORT||process.env.EMAIL_PORT||'465';const user=(process.env.SMTP_USER||process.env.SMTP_USERNAME||process.env.EMAIL_USER||process.env.GMAIL_USER||'').trim();const pass=(process.env.SMTP_PASS||process.env.SMTP_PASSWORD||process.env.EMAIL_PASS||process.env.GMAIL_APP_PASSWORD||'').trim().replace(/\s+/g,'');res.json({configured:Boolean(user&&pass),details:{host,port,user:user?`${user.slice(0,3)}***@${user.split('@')[1]||''}`:'',hasPassword:Boolean(pass),passwordLength:pass.length,secure:port==='465',from:getFromAddress()}});});
app.post('/api/smtp/test',requireAuth,requirePermission('administrar_configuracion'),async(req,res)=>{try{const to=String(req.body?.targetEmail||'sistemas@dimer.com.mx');const result=await sendEmail({to,subject:`[PRUEBA] SMTP Dimer ${new Date().toISOString()}`,html:`<p>Prueba SMTP Dimer exitosa.</p><p>${new Date().toLocaleString('es-MX')}</p>`});res.json({...result,targetEmail:to});}catch(e){err(res,e);}});
app.get('/api/stats',requireAuth,async(_req,res)=>{try{const rs=await getPopulatedRequests();const total=(s:string)=>rs.filter(r=>r.status===s).length;res.json({totalRequests:rs.length,pendingApproval:total('PENDIENTE_APROBACION'),approved:total('APROBADA'),paid:total('PAGADA'),rejected:total('RECHAZADA'),correctionRequested:total('CORRECCION_SOLICITADA'),totalAmountRequested:rs.reduce((a,r)=>a+r.amountRequested,0),totalAmountAuthorized:rs.reduce((a,r)=>a+(r.amountAuthorized||0),0),totalUsers:(await listUsers()).length,totalDepartments:(await listDepartments()).length,totalBosses:(await listBosses()).length,totalRoles:(await listRoles()).length});}catch(e){err(res,e);}});
app.get('/api/code-artifacts',async(_req,res)=>{try{const {NEXTJS_CODE_ARTIFACTS}=await import('./nextjsArtifacts');res.json(NEXTJS_CODE_ARTIFACTS);}catch(e){err(res,e);}});
app.all('/api/*',(_req,res)=>res.status(404).json({error:'Ruta API no encontrada'}));
app.use((e:unknown,_req:express.Request,res:express.Response,_next:express.NextFunction)=>{console.error('[DIMER]',e);if(!res.headersSent)res.status(500).json({success:false,error:'Error interno del servidor'});});return app;}
